import SystemHealth from "../apps/SystemHealth";

describe("SystemHealth App Component Unit Tests", () => {
  it("should export SystemHealth component function", () => {
    expect(typeof SystemHealth).toBe("function");
  });
});
